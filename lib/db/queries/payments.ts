// Phase 0 stub — payment queries land in Phase 2.
// lib/payments/ holds the simulated payment logic + the shared
// PaymentResult contract that lets future real MoMo/Orange Money
// providers swap in without touching the order flow.
export {};
