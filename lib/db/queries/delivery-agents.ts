// Phase 0 stub — delivery-agent record queries land in Phase 4
// (admin-only management of contact-only agent records). The agent
// portal itself remains dormant until post-MVP per security.md.
export {};
